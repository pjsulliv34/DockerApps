build:
  docker:
    web: Dockerfile

# Optional: If you need to run any commands after the build is complete
# release:
#   command:
#     - python manage.py migrate

run:
  web: gunicorn webapp.app:app --bind 0.0.0.0:$PORT
